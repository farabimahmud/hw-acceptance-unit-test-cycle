%h3 Movies by the same director of #{@movie.title}
%ol
  - @same_director_movies.each do |movie| 
    %li
      = movie.title 
      = link_to "More about #{movie.title}", movie_path(movie)
= link_to 'Back', movies_path